import { FileSignature } from 'lucide-react';
import { createToolPage } from './toolFactory';
export const SignToolPage = createToolPage({ icon: FileSignature, translationKey: 'sign', color: 'bg-cyan-600' });
