import { Scissors } from 'lucide-react';
import { createToolPage } from './toolFactory';
export const SplitToolPage = createToolPage({ icon: Scissors, translationKey: 'split', color: 'bg-sky-500' });
