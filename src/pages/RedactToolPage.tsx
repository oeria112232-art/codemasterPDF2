import { ShieldAlert } from 'lucide-react';
import { createToolPage } from './toolFactory';
export const RedactToolPage = createToolPage({ icon: ShieldAlert, translationKey: 'redact', color: 'bg-black' });
