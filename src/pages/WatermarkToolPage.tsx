import { FileBadge } from 'lucide-react';
import { createToolPage } from './toolFactory';
export const WatermarkToolPage = createToolPage({ icon: FileBadge, translationKey: 'watermark', color: 'bg-blue-600' });
