import { Divide } from 'lucide-react';
import { createToolPage } from './toolFactory';
export const OrganizeToolPage = createToolPage({ icon: Divide, translationKey: 'organize', color: 'bg-teal-600' });
